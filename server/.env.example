# Where uploads and generated files are stored
UPLOAD_DIR=uploads
# If LibreOffice is installed and you want .doc ➜ .docx conversion
SOFFICE_PATH=/usr/bin/soffice
# Optional: absolute path to tesseract executable if not in PATH
TESSERACT_CMD=/usr/bin/tesseract